{
  "ConnectionStrings": {
    "DefaultConnection": "Host=${HOST_POSTGRES};Port=${PORT_POSTGRES};Username=${USERNAME_POSTGRES};Password=${PASSWORD_POSTGRES};Database=${DATA_POSTGRES}"
  },
  "AppSettings": {
    "HOST_POSTGRES": "*",
    "PORT_POSTGRES": "*",
    "USERNAME_POSTGRES": "*",
    "PASSWORD_POSTGRES": "*",
    "DATA_POSTGRES": "*"
  }
}
